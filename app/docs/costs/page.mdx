export const metadata = {
  title: 'Costs',
};

# Costs

One price, and it covers the whole product — every context, business and
personal alike.

| Item | Cost |
| --- | --- |
| Thoughtstead (hosted) | **$20/month** |
| Hosting | Included |
| AI usage — embeddings, enrichment, search | Included, at no markup |
| Exporting your data | Free, any time |

There is no API key to obtain, no cloud account to create, no server to pay
for, and no per-token bill to watch. Adding a context — a second company, a
side project, a household — does not change the price. If you cancel, you can
still export everything first.

## What happens if something degrades

Thoughtstead never loses a capture. Every thought saves immediately, raw, before
any AI touches it. If enrichment or semantic search is temporarily degraded, the
app says so plainly on every AI surface, and the backlog processes automatically
once service is restored.

## Next

- [Privacy](/privacy) — what we do and do not do with your data.
